# Two Sum

# Дан массив целых чисел nums и целое число target.
# Необходимо вернуть индексы двух чисел, сумма которых равна target.
# Можно считать, что для каждого входного массива существует ровно одно решение,
# и один и тот же элемент нельзя использовать дважды.

# Задача (коротко).
# Нам нужно:
# найти два разных элемента в массиве nums,
# чтобы их сумма была равна target
# и вернуть их индексы.
