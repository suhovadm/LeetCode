# Complement of Base 10 Integer

# Дополнение целого числа - это целое число, которое получается путём замены всех 0 на 1
# и всех 1 на 0 в его двоичном представлении.

# Например, целое число 5 в двоичной системе представляется как "101", а его дополнение - "010",
# что соответствует целому числу 2.
# Для заданного числа n верните его дополнение.

# Пример 1:
# Вход: n = 5
# Выход: 2
# Пояснение: 5 в двоичной системе - это "101", а его дополнение в двоичной системе - "010",
# что в десятичной системе равно 2.

# Пример 2:
# Вход: n = 7
# Выход: 0
# Пояснение: 7 в двоичной системе - это "111", а его дополнение в двоичной системе - "000",
# что в десятичной системе равно 0.

# Пример 3:
# Вход: n = 10
# Выход: 5
# Пояснение: 10 в десятичной системе - это "1010", а его дополнение в двоичной системе - "0101",
# что в десятичной системе равно 5.

# Заводим класс Solution, как того требует LeetCode.
class Solution:
    # Определеям метод bitwiseComplement, который принимает в себя ряд параметров.
    # self - ссылка на экземпляр класса.
    # n: int - целое число.
    # -> int - возвращает целое число (его дополнение).
    def bitwiseComplement(self, n: int) -> int:

    # Обрабатываем особый случай - число 0.
    # Если n = 0, его двоичное представление - "0".
    # Дополнение должно быть "1", что в десятичной системе равно 1.
    # Возвращаем 1 сразу, не выполняя дальнейший код.
        if n == 0:
            return 1

    # Создаём переменную mask со значением 1.
    # В двоичном виде: 1 - это один бит.
        mask = 1

    # Запускаем цикл, который будет выполняться, пока маска меньше исходного числа n.
    # Нам нужно создать маску, которая по длине (количеству бит) равна числу n.
    # Например, для n = 5 (двоичное 101 - 3 бита) маска должна стать 111 (тоже 3 бита).
        while mask < n:
    # Увеличиваем длину маски на 1 бит.
    # mask << 1 - сдвигаем все биты маски влево на 1 позицию (добавляем 0 справа).
    # | 1 - устанавливаем самый правый (младший) бит в 1 с помощью операции OR.
    # В результате каждый раз добавляем справа еденицу, и маска становится последовательностью
    # из всех едениц возрастающей длины.
    # Например для n = 5 (101):
    # итерация 1: mask = 1 (1 бит),
    # проверка: 1 < 5 - true,
    # mask = (1 << 1) | 1 = 2 | 1 = 3 (в двоичной системе 11 - 2 бита).
    # Итерация 2: mask = 3 (2 бита),
    # проверка: 3 < 5 - true,
    # mask = (3 << 1) | 1 = 6 | 1 = 7 (в двоичной системе 111 - 3 бита).
    # Итерация 3: mask = 7 (3 бита),
    # проверка: 7 < 5 - false - цикл завершён.
            mask = (mask << 1) | 1

    # Возвращаем результат операции XOR (исключающее ИЛИ) между исходным числом и маской.
    # XOR работает так: 0 ^ 1 = 1, 1 ^ 1 = 0
    # Это идеально подходит для инвертирования битов:
    # - там, где в маске стоит 1, бит числа инвертируется,
    # - там, где в маске стоит 0, бит числа остаётся без изменений.
    # Поскольку маска состоит из всех едениц в значащих битах числа n, все биты
    # числа инвертируются.
    # Пример для n = 5:
    # n = 101 (двоичная),
    # mask = 111 (двоичная),
    # 101 ^ 111 = 010 (двоичная) = 2 (десятичная).
        return n ^ mask

# Создаем экземпляр класса
solution = Solution()

# ---------- Пример 1: n = 5 ----------
n1 = 5
print("=" * 60)
print("ПРИМЕР 1: n = 5")
print("=" * 60)

print("ШАГ 1: Проверяем особый случай")
print(f"   n = {n1} ≠ 0, поэтому продолжаем")
print()

print("ШАГ 2: Создаем маску")
print(f"   Начальная маска = 1 (двоичная: {bin(1)[2:]})")
mask = 1
print(f"   Проверяем: {mask} < {n1}? {'Да' if mask < n1 else 'Нет'}")

# Итерация 1
print(f"   Итерация 1: mask = ({mask} << 1) | 1 = {mask << 1} | 1 = {(mask << 1) | 1}")
mask = (mask << 1) | 1
print(f"      mask стала {mask} (двоичная: {bin(mask)[2:]})")
print(f"      Проверяем: {mask} < {n1}? {'Да' if mask < n1 else 'Нет'}")

# Итерация 2
print(f"   Итерация 2: mask = ({mask} << 1) | 1 = {mask << 1} | 1 = {(mask << 1) | 1}")
mask = (mask << 1) | 1
print(f"      mask стала {mask} (двоичная: {bin(mask)[2:]})")
print(f"      Проверяем: {mask} < {n1}? {'Да' if mask < n1 else 'Нет'}")
print(f"   Цикл завершен. Итоговая маска = {mask} (двоичная: {bin(mask)[2:]})")
print()

print("ШАГ 3: Применяем XOR для получения дополнения")
print(f"   {n1} в двоичной системе:  {bin(n1)[2:].zfill(len(bin(mask)[2:]))}")
print(f"   {mask} в двоичной системе: {bin(mask)[2:]}")
print(f"   {' ' * len(str(n1))}  {' ' * (len(bin(n1)[2:]) - len(bin(mask)[2:]))}  {'^' * len(bin(mask)[2:])}")
result1 = solution.bitwiseComplement(n1)
print(f"   Результат XOR: {bin(result1)[2:].zfill(len(bin(mask)[2:]))} (двоичная)")
print(f"   {bin(result1)[2:]} в десятичной системе = {result1}")
print()

print("=" * 60)
print("ПРИМЕР 2: n = 7")
print("=" * 60)

# ---------- Пример 2: n = 7 ----------
n2 = 7
print("ШАГ 1: Проверяем особый случай")
print(f"   n = {n2} ≠ 0, поэтому продолжаем")
print()

print("ШАГ 2: Создаем маску")
print(f"   Начальная маска = 1 (двоичная: {bin(1)[2:]})")
mask = 1
print(f"   Проверяем: {mask} < {n2}? {'Да' if mask < n2 else 'Нет'}")

# Итерация 1
print(f"   Итерация 1: mask = ({mask} << 1) | 1 = {mask << 1} | 1 = {(mask << 1) | 1}")
mask = (mask << 1) | 1
print(f"      mask стала {mask} (двоичная: {bin(mask)[2:]})")
print(f"      Проверяем: {mask} < {n2}? {'Да' if mask < n2 else 'Нет'}")

# Итерация 2
print(f"   Итерация 2: mask = ({mask} << 1) | 1 = {mask << 1} | 1 = {(mask << 1) | 1}")
mask = (mask << 1) | 1
print(f"      mask стала {mask} (двоичная: {bin(mask)[2:]})")
print(f"      Проверяем: {mask} < {n2}? {'Да' if mask < n2 else 'Нет'}")

# Итерация 3
print(f"   Итерация 3: mask = ({mask} << 1) | 1 = {mask << 1} | 1 = {(mask << 1) | 1}")
mask = (mask << 1) | 1
print(f"      mask стала {mask} (двоичная: {bin(mask)[2:]})")
print(f"      Проверяем: {mask} < {n2}? {'Да' if mask < n2 else 'Нет'}")
print(f"   Цикл завершен. Итоговая маска = {mask} (двоичная: {bin(mask)[2:]})")
print()

print("ШАГ 3: Применяем XOR для получения дополнения")
print(f"   {n2} в двоичной системе: {bin(n2)[2:]}")
print(f"   {mask} в двоичной системе: {bin(mask)[2:]}")
print(f"   {' ' * len(str(n2))}  {'^' * len(bin(mask)[2:])}")
result2 = solution.bitwiseComplement(n2)
print(f"   Результат XOR: {bin(result2)[2:].zfill(len(bin(mask)[2:]))} (двоичная)")
print(f"   {bin(result2)[2:]} в десятичной системе = {result2}")
print()

print("=" * 60)
print("ПРИМЕР 3: n = 10")
print("=" * 60)

# ---------- Пример 3: n = 10 ----------
n3 = 10
print("ШАГ 1: Проверяем особый случай")
print(f"   n = {n3} ≠ 0, поэтому продолжаем")
print()

print("ШАГ 2: Создаем маску")
print(f"   Начальная маска = 1 (двоичная: {bin(1)[2:]})")
mask = 1
print(f"   Проверяем: {mask} < {n3}? {'Да' if mask < n3 else 'Нет'}")

# Итерация 1
print(f"   Итерация 1: mask = ({mask} << 1) | 1 = {mask << 1} | 1 = {(mask << 1) | 1}")
mask = (mask << 1) | 1
print(f"      mask стала {mask} (двоичная: {bin(mask)[2:]})")
print(f"      Проверяем: {mask} < {n3}? {'Да' if mask < n3 else 'Нет'}")

# Итерация 2
print(f"   Итерация 2: mask = ({mask} << 1) | 1 = {mask << 1} | 1 = {(mask << 1) | 1}")
mask = (mask << 1) | 1
print(f"      mask стала {mask} (двоичная: {bin(mask)[2:]})")
print(f"      Проверяем: {mask} < {n3}? {'Да' if mask < n3 else 'Нет'}")

# Итерация 3
print(f"   Итерация 3: mask = ({mask} << 1) | 1 = {mask << 1} | 1 = {(mask << 1) | 1}")
mask = (mask << 1) | 1
print(f"      mask стала {mask} (двоичная: {bin(mask)[2:]})")
print(f"      Проверяем: {mask} < {n3}? {'Да' if mask < n3 else 'Нет'}")

# Итерация 4
print(f"   Итерация 4: mask = ({mask} << 1) | 1 = {mask << 1} | 1 = {(mask << 1) | 1}")
mask = (mask << 1) | 1
print(f"      mask стала {mask} (двоичная: {bin(mask)[2:]})")
print(f"      Проверяем: {mask} < {n3}? {'Да' if mask < n3 else 'Нет'}")
print(f"   Цикл завершен. Итоговая маска = {mask} (двоичная: {bin(mask)[2:]})")
print()

print("ШАГ 3: Применяем XOR для получения дополнения")
print(f"   {n3} в двоичной системе:  {bin(n3)[2:].zfill(len(bin(mask)[2:]))}")
print(f"   {mask} в двоичной системе: {bin(mask)[2:]}")
print(f"   {' ' * len(str(n3))}  {' ' * (len(bin(n3)[2:]) - len(bin(mask)[2:]))}  {'^' * len(bin(mask)[2:])}")
result3 = solution.bitwiseComplement(n3)
print(f"   Результат XOR: {bin(result3)[2:].zfill(len(bin(mask)[2:]))} (двоичная)")
print(f"   {bin(result3)[2:]} в десятичной системе = {result3}")